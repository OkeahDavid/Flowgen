# API Configuration
# For local development, use localhost
VITE_API_URL=http://localhost:8000

# Demo token for production API authentication
# Leave empty for local development (backend runs in open mode)
# Set this in Netlify environment variables for production
VITE_DEMO_TOKEN=
